# Tasks

- [x] Add root Responses HTTP/WebSocket aliases and regression tests for 405 compatibility.
- [x] Fix settings responsive layout and refresh handling for nested settings queries; add UI tests.
- [x] Isolate account health write failures from active user sessions and add regression coverage.
- [x] Run targeted tests and frontend checks. OpenSpec CLI is unavailable in this environment; validate manually via the checked-in artifacts.
